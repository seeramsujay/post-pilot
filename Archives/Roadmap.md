# Roadmap: PostPilot

## Legend
**DONE** | **ACTIVE** | **BLOCKED** | **COLAB** | **LOCAL** | **RISK**

---

## Phases

### Phase 1 — Remote Environment & Scaffold [LOCAL]
**Goal:** Initialize the Devvit Web topology on the SSH PC and secure platform permissions.
**Depends:** none

- [ ] Execute `npx devvit new postpilot --template react` via SSH terminal to generate the Vite-based topography.
  - *Constraint:* Must use React template; legacy Blocks are deprecated.
  - *Colab:* no
- [ ] Purge default counter boilerplate from `src/client` and `src/server`.
  - *Colab:* no
- [ ] Inject application settings schema (`title_regex`, `min_body`, `flair_id`, `enforcer`) directly into the root of `devvit.json`.
  - *Constraint:* Do not use `Devvit.addSettings()` in TypeScript; it causes schema validation failures.
  - *Colab:* no
- [ ] Configure `devvit.json` permissions for `redis` and `reddit` (`SUBMIT_POST`, `SUBMIT_COMMENT` inside `asUser`).
  - *Risk:* Missing `asUser` scopes will cause silent API failures during post submission.
  - *Colab:* no

### Phase 2 — Server Architecture and Redis Schema [LOCAL]
**Goal:** Establish API endpoints and highly efficient Redis data structures.
**Depends:** 1

- [ ] Construct `GET /api/rules` endpoint in `src/server/index.ts` mapping `context.settings.get()` to JSON.
  - *Colab:* no
- [ ] Establish Redis Hash structure for subreddit configurations to ensure single `HGETALL` read access.
  - *Constraint:* Avoid individual keys for rules to prevent Reddit Redis quota exhaustion and latency spikes.
  - *Colab:* no
- [ ] Scaffold `POST /api/publish` endpoint with base request parsing.
  - *Colab:* no

### Phase 3 — Client-Side State Machine & Composer UI [LOCAL]
**Goal:** Build the React webview for isolated, zero-latency draft validation.
**Depends:** 2

- [ ] Architect flat state machine using `useState<MachineState>` (`INITIALIZING`, `DRAFTING`, `VALIDATING`, `SUBMITTING`, `SUCCESS`, `ERROR`).
  - *Colab:* no
- [ ] Implement `useEffect` to fetch rules from `/api/rules` on component mount.
  - *Colab:* no
- [ ] Develop drafting interface with `<input>` for title and `<textarea>` for body text.
  - *Colab:* no
- [ ] Implement live Markdown preview pane adjacent to the drafting textarea.
  - *Colab:* no
- [ ] Implement debounced, client-side regex and word-count validation triggered by `onChange` events.
  - *Colab:* no
- [ ] Implement `onPaste` clipboard interception to strip HTML and enforce plain-text/Markdown.
  - *Risk:* Strictly prevent base64 image pasting to avoid breaching the 4MB maximum request size limit.
  - *Colab:* no

### Phase 4 — Secure Publishing Pipeline & Idempotency [LOCAL]
**Goal:** Connect the frontend to the Reddit API safely without triggering 429 rate limits.
**Depends:** 3

- [ ] Implement Redis NX idempotency lock inside `POST /api/publish` using a UUID generated by the client.
  - *Constraint:* Set lock expiration to 15 seconds to prevent rate-limit exhaustion from concurrent clicks.
  - *Colab:* no
- [ ] Execute `reddit.submitPost` utilizing `runAs: 'USER'` and capture the returned Post ID.
  - *Constraint:* Requires `userGeneratedContent` property mapping to the drafted text for Reddit safety scans.
  - *Colab:* no
- [ ] Execute `reddit.setPostFlair` asynchronously using the captured Post ID.
  - *Risk:* Applying flair within the initial `submitPost` payload is bugged on Reddit's end; separation is mandatory.
  - *Colab:* no
- [ ] Write `portal_post:${post.id}` to Redis with a 1-hour expiration to flag the submission as verified.
  - *Colab:* no

### Phase 5 — Global Fallback Enforcer [LOCAL]
**Goal:** Intercept and remove native posts that bypass the webview portal.
**Depends:** 4

- [ ] Bind `Devvit.addTrigger({ event: 'PostSubmit' })` in `src/server/index.ts` to intercept native post submissions.
  - *Colab:* no
- [ ] Validate incoming `postId` against the `portal_post` Redis flag.
  - *Colab:* no
- [ ] Execute `reddit.remove()` for unverified posts.
  - *Colab:* no
- [ ] Execute `reddit.submitComment()` with distinguished moderator styling to redirect users to the portal.
  - *Risk:* Ensure the trigger respects the `enforcer_enabled` global toggle to prevent accidental mass-removals during maintenance.
  - *Colab:* no

### Phase 6 — Advanced Rule Engine Expansion [LOCAL]
**Goal:** Expand the portal beyond the MVP to handle complex, production-grade subreddit rules.
**Depends:** 5

- [ ] Expand `devvit.json` to include `keyword_blacklist` (comma-separated string) and `minimum_account_age` (number) settings.
  - *Colab:* no
- [ ] Implement client-side blacklist scanning to instantly render inline errors for forbidden words.
  - *Colab:* no
- [ ] Implement backend account age and karma validation in `POST /api/publish` via `reddit.getUser()`.
  - *Constraint:* Do not trust the client for user metadata; fetch authoritative user data backend-side before publishing.
  - *Colab:* no
- [ ] Map backend validation failures to HTTP 403 error states and render specific UI warnings in the React client.
  - *Colab:* no

### Phase 7 — Telemetry & Analytics Dashboard [LOCAL]
**Goal:** Provide moderators with persistent data on portal effectiveness and rule triggers.
**Depends:** 6

- [ ] Instrument backend to increment Redis counters (`stats:approved`, `stats:rejected`, `stats:enforcer_bypassed`).
  - *Colab:* no
- [ ] Construct `GET /api/stats` endpoint to aggregate and serve telemetry data.
  - *Colab:* no
- [ ] Create a secondary React view specifically for moderators to view analytics charts.
  - *Colab:* no
- [ ] Add `Devvit.addMenuItem` labeled "PostPilot Analytics" to spawn the dashboard in a private, mod-only context.
  - *Constraint:* Ensure the analytics webview restricts access by verifying the user's moderator status via `context.reddit.getModerators()`.
  - *Colab:* no

### Phase 8 — Production Hardening & Optimization [LOCAL]
**Goal:** Finalize the application for high-traffic subreddits and unpredictable network conditions.
**Depends:** 7

- [ ] Optimize Vite build pipeline to strip unused React DOM attributes and minimize the production bundle.
  - *Constraint:* Final client bundle must remain strictly under 4MB to pass App Review.
  - *Colab:* no
- [ ] Implement React Error Boundaries to catch webview crashes and display a graceful reload prompt.
  - *Colab:* no
- [ ] Implement client-side retry logic with exponential backoff for `/api/publish` to handle transient network drops.
  - *Colab:* no
- [ ] Execute final `npm run build`, `devvit upload`, and cross-platform QA testing (iOS, Android, Desktop Web).
  - *Colab:* no
